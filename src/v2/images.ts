export async function compressImage(file:File,maxDimension=1600,targetBytes=700_000):Promise<File>{if(!file.type.startsWith("image/"))throw new Error("Choose an image file.");const bitmap=await createImageBitmap(file);const ratio=Math.min(1,maxDimension/Math.max(bitmap.width,bitmap.height));const canvas=document.createElement("canvas");canvas.width=Math.round(bitmap.width*ratio);canvas.height=Math.round(bitmap.height*ratio);const context=canvas.getContext("2d");if(!context)throw new Error("Image processing is unavailable.");context.drawImage(bitmap,0,0,canvas.width,canvas.height);let quality=.82;let blob:Blob|null=null;while(quality>=.45){blob=await new Promise(resolve=>canvas.toBlob(resolve,"image/jpeg",quality));if(blob&&blob.size<=targetBytes)break;quality-=.08;}if(!blob)throw new Error("Could not compress this image.");return new File([blob],`${crypto.randomUUID()}.jpg`,{type:"image/jpeg"});}

