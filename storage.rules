rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /spaces/{spaceId}/ideas/{ideaId}/{fileName} {
      allow read, write: if request.auth != null && firestore.get(/databases/(default)/documents/spaces/$(spaceId)).data.memberIds.hasAny([request.auth.uid]);
    }
  }
}
